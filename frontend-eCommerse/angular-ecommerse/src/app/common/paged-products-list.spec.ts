import { PagedProductsList } from './paged-products-list';

describe('PagedProductsList', () => {
  it('should create an instance', () => {
    expect(new PagedProductsList()).toBeTruthy();
  });
});
